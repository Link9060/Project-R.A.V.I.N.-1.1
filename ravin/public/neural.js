/* Accent signal layer: activity originates at the Core and branches through the field. */
(()=>{
 const canvas=document.createElement("canvas");canvas.id="neuralEnergy";document.body.appendChild(canvas);const ctx=canvas.getContext("2d");let w=0,h=0,dpr=1,raf=0;let active=false;let pulses=[];let nodes=[];const core=()=>document.getElementById("core");
 function resize(){dpr=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;canvas.width=w*dpr;canvas.height=h*dpr;canvas.style.width=w+"px";canvas.style.height=h+"px";ctx.setTransform(dpr,0,0,dpr,0,0);nodes=Array.from({length:Math.max(20,Math.min(48,Math.floor(w*h/32000)))},()=>({x:Math.random()*w,y:Math.random()*h}));}
 function origin(){const r=core()?.getBoundingClientRect();return r?{x:r.left+r.width/2,y:r.top+r.height/2}:{x:w/2,y:h/2}}
 function spawn(){const o=origin();for(let i=0;i<5;i++){const n=nodes[Math.floor(Math.random()*nodes.length)];pulses.push({sx:o.x,sy:o.y,tx:n.x,ty:n.y,t:Math.random()*.35,d:.7+Math.random()*.55,branch:Math.random()>.45})}}
 function draw(t){ctx.clearRect(0,0,w,h);if(active&&Math.random()<.08)spawn();for(let i=pulses.length-1;i>=0;i--){const p=pulses[i];p.t+=.016/p.d;if(p.t>1){pulses.splice(i,1);continue}const e=p.t<.5?p.t*2:2-p.t*2;const x=p.sx+(p.tx-p.sx)*p.t,y=p.sy+(p.ty-p.sy)*p.t;ctx.beginPath();ctx.moveTo(p.sx,p.sy);ctx.lineTo(p.tx,p.ty);ctx.strokeStyle=`rgba(143,167,255,${.04*e})`;ctx.lineWidth=1;ctx.stroke();ctx.beginPath();ctx.arc(x,y,2.1+e*2,0,Math.PI*2);ctx.fillStyle=`rgba(143,167,255,${.7*e})`;ctx.shadowBlur=12;ctx.shadowColor="rgba(143,167,255,.8)";ctx.fill();ctx.shadowBlur=0}raf=requestAnimationFrame(draw)}
 window.addEventListener("resize",resize);window.addEventListener("ravin-neural",e=>{active=!!e.detail?.active;if(active)spawn()});resize();raf=requestAnimationFrame(draw);
})();
