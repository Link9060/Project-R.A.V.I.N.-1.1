/* RAVIN signal layer. Signals travel across the exact nodes/links already drawn by particles.js. */
(()=>{const canvas=document.createElement("canvas");canvas.id="neuralEnergy";document.body.appendChild(canvas);const ctx=canvas.getContext("2d");let w=0,h=0,dpr=1,active=false,pulses=[];const core=()=>document.getElementById("core");function color(){const c=getComputedStyle(document.documentElement).getPropertyValue("--accent").trim()||"#8fa7ff",n=parseInt(c.slice(1),16);return[(n>>16)&255,(n>>8)&255,n&255]}function resize(){dpr=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;canvas.width=w*dpr;canvas.height=h*dpr;canvas.style.width=w+"px";canvas.style.height=h+"px";ctx.setTransform(dpr,0,0,dpr,0,0)}function origin(){const r=core()?.getBoundingClientRect();return r?{x:r.left+r.width/2,y:r.top+r.height/2}:{x:w/2,y:h/2}}function nearest(nodes,x,y){let best=null,bd=Infinity;for(const n of nodes){const d=Math.hypot(n.x-x,n.y-y);if(d<bd){bd=d;best=n}}return best}function buildPath(){const nodes=window.ravinField?.getNodes?.()||[];if(!nodes.length)return[];const o=origin();let current=nearest(nodes,o.x,o.y);if(!current)return[];const path=[{x:o.x,y:o.y},current];const used=new Set([current]);for(let depth=0;depth<7;depth++){let next=null,score=Infinity;for(const n of nodes){if(used.has(n))continue;const d=Math.hypot(n.x-current.x,n.y-current.y);if(d<115&&d<score){score=d;next=n}}if(!next)break;used.add(next);path.push(next);current=next}return path}function spawn(){const path=buildPath();if(path.length<2)return;pulses.push({path,t:0,speed:.012+Math.random()*.008})}function draw(){ctx.clearRect(0,0,w,h);canvas.style.opacity=active?"1":"0";const [r,g,b]=color();if(active&&Math.random()<.045)spawn();for(let i=pulses.length-1;i>=0;i--){const p=pulses[i];p.t+=p.speed;if(p.t>=p.path.length-1){pulses.splice(i,1);continue}const a=Math.floor(p.t),f=p.t-a,A=p.path[a],B=p.path[a+1],x=A.x+(B.x-A.x)*f,y=A.y+(B.y-A.y)*f;ctx.beginPath();ctx.moveTo(A.x,A.y);ctx.lineTo(B.x,B.y);ctx.strokeStyle=`rgba(${r},${g},${b},.34)`;ctx.lineWidth=1.1;ctx.shadowBlur=10;ctx.shadowColor=`rgba(${r},${g},${b},.6)`;ctx.stroke();ctx.beginPath();ctx.arc(x,y,2.1,0,Math.PI*2);ctx.fillStyle=`rgba(${r},${g},${b},.95)`;ctx.shadowBlur=16;ctx.fill();ctx.shadowBlur=0}requestAnimationFrame(draw)}addEventListener("resize",resize);addEventListener("ravin-neural",e=>{active=!!e.detail?.active;if(active){for(let i=0;i<3;i++)setTimeout(spawn,i*180)}else setTimeout(()=>{pulses=[]},900)});resize();draw()})();